name: Build and Upload Artefact

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Create a file
        run: echo "Test workflow Armand Rémi" > artefact.txt

      - name: Upload artefact
        uses: actions/upload-artefact@v3
        with:
          name: my-artefact
          path: artefact.txt
